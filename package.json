{
  "name": "rancher-dns-internal",
  "version": "0.1.0",
  "description": "Service-discovery agent for exposing all services into DNS",
  "main": "bin/rancher-dns-discovery.js",
  "scripts": {
    "start": "node bin/rancher-dns-discovery.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/ -d dist"
  },
  "author": "Nick Delitski",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.7.0",
    "babel-core": "^5.8.33",
    "bluebird": "^3.0.5",
    "lodash": "^3.10.1",
    "native-dns": "^0.7.0"
  }
}
